# Simple Thread Pool in C

This project implements a simple thread‐pool in C using POSIX threads.

## Features
- Fixed number of worker threads
- Task queue
- Add tasks and wait for completion
- Clean shutdown

## How to build
```bash
make
